"""RetailForge — Multi-Agent Retail Intelligence System.

A Google ADK multi-agent backend (root orchestrator + product / recommendation /
support / billing sub-agents) backed by MongoDB Atlas via the MCP Toolbox, exposed
to a CopilotKit storefront over the AG-UI protocol.
"""

__version__ = "0.1.0"
