# Copy to `.env.local`.

# The AG-UI agent backend (FastAPI) the CopilotKit runtime proxies to.
AGUI_BACKEND_URL="http://localhost:8000/"

# The storefront read API (products / orders) used by server components.
READ_API_URL="http://localhost:8001"
